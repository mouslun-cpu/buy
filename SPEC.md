# 政府標案投標輔助系統 — 系統規格書 v1.0

> 文件目的：本規格書供未來的 AI Agent 直接依此建置系統。撰寫日期 2026-07-11。
> 業主背景：已成立資本額 NT$1,000 萬之公司，專營政府採購投標，方向以**勞務類採購**為主，但關鍵字條件需保持彈性。

---

## 0. 一頁摘要

建置一套「標案雷達 + 案件資料庫 + 投標決策分析」三合一系統：

1. **標案雷達**：每日自動抓取政府電子採購網新公告，依使用者自訂條件（關鍵字、金額、機關、標的分類）過濾，推播到 LINE 群組。
2. **案件資料庫**：所有推播過的案件入庫留痕，含預算金額、決標方式、時程等結構化欄位，可回溯查詢與追蹤狀態。
3. **決策分析**：對單一案件做自動可行性評估（資格門檻、押標金、決標方式、時程風險）與獲利粗估，產出「建議投/不投」報告輔助決策。

技術棧沿用 Allen 慣用組合：**Next.js + TypeScript + Tailwind + Firebase（Firestore）+ Zeabur 部署**，排程爬蟲跑在 Zeabur 上的獨立 Node.js worker（或 Zeabur cron），推播用 **LINE Messaging API**（注意：LINE Notify 已於 2025-03-31 終止服務，不可再用）。

---

## 1. 資料來源分析（已於 2026-07-11 實查驗證）

### 1.1 三個可用來源與定位

| 來源 | 內容 | 即時性 | 用途定位 |
|---|---|---|---|
| 政府電子採購網 官方 OpenData（`web.pcc.gov.tw/tps/tp/OpenData/showList`） | 招標資料、決標資料，XML 格式，政府資料開放授權 | **延遲約 2 個月**（每月 5 號產出 2 個月前資料，半月一檔） | 歷史資料回填、決標行情分析（誰得標、決標金額 vs 預算） |
| 政府電子採購網 網頁查詢（`web.pcc.gov.tw/prkms/tender/common/basic/indexTenderBasic` 等） | 當日招標公告、案件詳情頁（預算、決標方式、資格、時程） | 即時 | **每日新案偵測的主要來源**（需禮貌爬取，見 1.3） |
| g0v 標案 API（`pcc.g0v.ronny.tw`，openfunltd 維護） | 整理好的標案 JSON API，可依日期/機關/關鍵字查 | 接近即時 | **備援來源**。注意：2025-07 起因 AI 爬蟲濫用已上 Cloudflare 擋 bot，程式化存取可能被擋，整合前需實測；若可用則優先於自建爬蟲 |

### 1.2 來源策略（Agent 建置時依此順序）

1. **先實測 g0v API**：若以正常速率（如每日數十次請求、帶識別 UA）可穩定存取，每日新案偵測直接用它，省掉自建爬蟲的維護成本。
2. **不可用則自建爬蟲**：爬官方網頁查詢（公告日期＝當日、標的分類＝勞務等條件），只在每日固定時段跑 1–2 輪。
3. **官方 OpenData XML 每月定期匯入**：建立歷史招標＋決標資料庫，供分析模組使用（例如同類案件的歷史決標價格、常見得標廠商）。

### 1.3 反爬蟲紀律（硬性要求，違反會被封鎖）

- 每次 HTTP 請求間隔 **≥ 5 秒**（加隨機抖動 3–8 秒），單日總請求數設上限（建議 ≤ 300）。
- 每日僅在 1–2 個固定時段執行（建議 08:30、16:30 各一輪，涵蓋上午/下午新公告）。
- 帶固定且誠實的 User-Agent，失敗採指數退避（backoff），連續失敗 3 次即停止當輪並告警，**絕不重試轟炸**。
- 案件詳情頁只抓「通過關鍵字初篩的案件」，不做全量抓取。
- 快取所有已抓頁面（依案號＋公告日期為 key），同一頁絕不重複抓。

---

## 2. 系統架構

```
┌─────────────────────────────────────────────────┐
│ Zeabur                                          │
│                                                 │
│  ┌──────────────┐      ┌─────────────────────┐  │
│  │ Next.js Web  │      │ Worker (Node.js)    │  │
│  │ 管理後台      │      │ - 每日排程抓取       │  │
│  │ - 條件設定    │      │ - 過濾比對           │  │
│  │ - 案件列表    │      │ - LINE 推播          │  │
│  │ - 分析報告    │      │ - 月度 OpenData 匯入 │  │
│  └──────┬───────┘      └──────────┬──────────┘  │
└─────────┼─────────────────────────┼─────────────┘
          │                         │
          ▼                         ▼
   ┌─────────────────────────────────────┐
   │ Firebase Firestore                  │
   │ profiles / tenders / notifications  │
   │ / analyses / awards(歷史決標)        │
   └─────────────────────────────────────┘
          │
          ▼
   ┌──────────────────┐     ┌──────────────────┐
   │ LINE Messaging   │     │ Claude API        │
   │ API（群組推播）    │     │（公告解析＋分析）   │
   └──────────────────┘     └──────────────────┘
```

- **Web（Next.js）**：內部管理後台，不對外公開，簡單登入（Firebase Auth，白名單 email）。
- **Worker**：獨立 Node.js 服務或 Zeabur cron job，與 Web 分開部署，避免爬蟲拖垮後台。
- **LLM（Claude API）**：用於兩處——(a) 把非結構化的公告內文（資格條件、履約範圍）解析成結構化欄位；(b) 產生可行性評估報告初稿。模型建議 `claude-sonnet-5`（成本/品質平衡），解析類任務可降 `claude-haiku-4-5`。

---

## 3. 功能模組規格

### 模組 A：條件設定介面（彈性訂閱條件）

因投標方向未定，條件設計成多組「訂閱 Profile」，每組獨立推播、獨立統計命中率，方便試錯調整。

**單一 Profile 欄位：**

| 欄位 | 型別 | 說明 |
|---|---|---|
| name | string | 例：「清潔勞務」「資訊委外」 |
| keywords_include | string[] | 標案名稱命中任一即入選（OR） |
| keywords_require | string[] | 必須全部命中（AND，可空） |
| keywords_exclude | string[] | 命中任一即排除，例：「工程」「原住民」 |
| category | enum[] | 標的分類：勞務／財物／工程（預設只勾勞務） |
| budget_min / budget_max | number | 預算區間過濾（0＝不限） |
| org_include / org_exclude | string[] | 機關名稱包含/排除（例：只要北部機關） |
| tender_way | enum[] | 招標方式過濾：公開招標／公開取得報價單／限制性招標（可空＝全收） |
| region | string[] | 履約地點縣市過濾（可空） |
| line_target | string | 推播目的地（LINE 群組 ID），不同 Profile 可推不同群 |
| enabled | boolean | 開關 |

**介面需求：**
- Profile 的 CRUD 列表頁 + 編輯表單。
- 「回測」按鈕：用該條件對資料庫既有案件跑一遍，即時顯示過去 30 天會命中哪些案，讓使用者調關鍵字時有手感——**這是本模組最重要的 UX**，因為方向未定、條件會一直調。
- 每個 Profile 顯示近 30 天命中數。

### 模組 B：每日抓取＋推播

**流程（每日 08:30 / 16:30 各一輪）：**

1. 取得當日新公告清單（來源依 1.2 策略）。
2. 與 `tenders` collection 比對案號，過濾已處理案件。
3. 新案件逐一過所有 enabled Profile 的條件；命中任一 Profile 才抓詳情頁。
4. 解析詳情頁結構化欄位（見資料模型 `tenders`），LLM 輔助解析非結構化段落。
5. 寫入 Firestore，狀態 `new`。
6. 組 LINE 訊息推播到該 Profile 的群組，推播紀錄寫入 `notifications`。

**LINE 推播（Messaging API）：**
- 建立 LINE 官方帳號 → 開 Messaging API channel → 把官方帳號拉進目標群組 → 透過 webhook 事件取得 `groupId`。
- 用 Push Message API 發 **Flex Message** 卡片，每案一張，欄位：案名、機關、**預算金額**、決標方式、截止投標日（含倒數天數）、命中的 Profile 名、案件詳情連結、後台分析頁連結。
- 免費額度每月 200 則（以 LINE 官方帳號方案為準），超過需升級方案——訊息量預估要在條件調校時注意，或多案合併成一則摘要訊息（建議：單輪命中 >5 案時自動改用「列表式摘要一則」）。
- Webhook 同時支援簡單指令：在群組輸入「分析 <案號>」回傳該案分析摘要（Phase 3）。

### 模組 C：案件紀錄

- 所有命中案件永久入庫，列表頁支援：狀態篩選、Profile 篩選、日期區間、關鍵字搜尋、預算排序。
- 案件狀態機：`new`（新推播）→ `watching`（考慮中）→ `analyzing`（分析中）→ `go`（決定投標）→ `no_go`（放棄，需填一句原因）→ `submitted` → `won` / `lost`。
- 決標追蹤：Worker 每日順帶查詢狀態在 `submitted` 的案件是否已決標；月度 OpenData 匯入時，自動把決標結果（得標廠商、決標金額）回填到對應案件——**這是累積自家報價策略資料的關鍵**（我們輸掉的案，對手用多少錢拿走）。

### 模組 D：可行性評估與獲利推估

> 定位務實：公開頁面資訊足以做「初篩級」評估（擋掉明顯不能投/不值得投的案），但**完整評估必須讀招標文件**（需領標，部分文件要付費電子領標）。系統分兩層：

**D1 初篩（自動，公開資訊即可）——輸出 0–100 分 + 紅黃綠燈：**

| 檢核項 | 邏輯 |
|---|---|
| 資格門檻 | 解析公告資格條件：實收資本額要求 vs 本公司 1,000 萬；特定證照/登記（如乙級清潔、勞派登記）；同類實績要求（新公司通常沒有 → 直接紅燈項） |
| 決標方式 | 最低標＝拚價格；最有利標／評分及格最低標＝拚企劃與簡報，對新公司相對有機會但備標成本高。標示並納入評分 |
| 押標金/保證金 | 金額與占預算比（法定上限為預算 5% 上下，以公告為準），評估資金占用 |
| 時程 | 公告到截止的備標天數（<7 天且需服務建議書 → 黃燈）；履約期程 |
| 預算合理性 | 對照歷史決標庫同類案件的「決標金額/預算比」分布，判斷該預算是否偏緊 |
| 競爭密度 | 歷史上同機關同類案的投標家數、常客得標廠商（來自決標庫） |
| 電子領投標 | 是否允許電子投標（影響作業流程） |

**D2 獲利粗估（半自動）：**
- 勞務案成本模型模板：人力（人月數 × 月薪 × 勞健保雇主負擔係數 ≈ 1.15–1.20）＋管理費 ％ ＋ 保證金資金成本 ＋ 備標成本。
- 系統從公告/招標文件抽出「人力需求數、履約月數」帶入模板，使用者可在介面上調參數，即時算毛利率。
- 搭配歷史決標行情：「同類案平均以預算 87% 決標 → 若你要贏最低標，估你的成本上限是 X」。
- **原則：所有推估值明確標示為推估，來源欄位標示（公告抽取／使用者輸入／歷史統計），不得呈現為確定值。**

**D3 分析報告產出：**
- 每案一頁分析頁：D1 檢核表 + D2 試算器 + LLM 生成的文字摘要（案件重點、風險、建議），一鍵推播摘要回 LINE 群組。

### 模組 E：採購法規則引擎（D1 的基礎）

把政府採購法的關鍵門檻寫成設定檔（不寫死在程式），因金額門檻會修訂：

```yaml
# thresholds.yaml（建置時需向工程會最新公告核對數字）
公告金額: 1500000        # 2023-01-01 起調整為 150 萬
查核金額_勞務: 10000000
巨額_勞務: 20000000
小額採購: 150000
押標金上限比率: 0.05
```

規則舉例：
- 未達公告金額（<150 萬）案多用「公開取得報價單或企劃書」，程序較簡便、對新公司友善 → 初篩加分。
- 達查核金額（勞務 ≥1,000 萬）受上級機關監督、程序長 → 標示。
- 決標方式類型辨識：最低標／最有利標／評分及格最低標／複數決標／限制性招標準用最有利標評選（採購法 §22-1-9 專業服務常見）。
- **法規門檻數字建置時一律以行政院公共工程委員會最新公告為準，本文件數字僅供 schema 設計參考。**

---

## 4. 資料模型（Firestore）

```
profiles/{profileId}        # 模組 A 的訂閱條件
tenders/{tenderId}          # key = 標案案號+機關代碼 hash
  ├ job_number, title, org_name, org_id
  ├ category(勞務/財物/工程), procurement_law_category
  ├ budget (number, 元), budget_disclosed (bool)
  ├ tender_way(招標方式), award_way(決標方式: lowest/most_advantageous/...)
  ├ publish_date, deadline(截止投標), open_date(開標)
  ├ perform_location, perform_period(履約期程)
  ├ bond_amount(押標金), e_bid(電子投標可否)
  ├ qualification_raw(資格原文), qualification_parsed{} (LLM 解析)
  ├ source_url, matched_profiles[], status(狀態機), status_note
  └ raw_snapshot(原始頁面存檔參照)
notifications/{id}          # 推播紀錄: tenderId, profileId, line_target, sent_at, message_type
analyses/{tenderId}         # D1 checklist 結果, D2 參數與試算, LLM 報告, 版本
awards/{awardId}            # 月度 OpenData 匯入的歷史決標: 案號, 機關, 預算, 決標金額, 得標廠商[], 投標家數, 決標日
settings/global             # thresholds.yaml 內容, 爬蟲參數, LINE channel 設定
```

---

## 5. 分期建置計畫（給執行 Agent 的順序）

### Phase 1 — 標案雷達 MVP（先讓每天有案子進群組）
1. 專案初始化：Next.js + Firebase + Zeabur 部署管線（用 `new-project-deploy` skill）。
2. 資料來源實測：g0v API 可用性測試 → 決定主來源；實作抓取器＋反爬蟲紀律（1.3）。
3. Profile CRUD（先做最簡表單，含關鍵字 include/exclude、分類、預算區間）。
4. LINE Messaging API 接通：官方帳號建立（需 Allen 手動申請）、webhook 取 groupId、Flex Message 推播。
5. 每日排程跑通，`tenders`/`notifications` 入庫。
   **驗收：連續 5 個工作天，每天新的勞務類案件依條件準時推入 LINE 群組，含預算金額，無重複推播，未觸發封鎖。**

### Phase 2 — 案件資料庫與後台
1. 案件列表頁＋狀態機＋回測功能。
2. 月度 OpenData XML 匯入器，回填 12–24 個月歷史招標＋決標資料到 `awards`。
3. 決標結果自動回填。
   **驗收：可回答「過去一年 XX 機關的清潔勞務案，平均決標金額是預算的幾成、誰常得標」。**

### Phase 3 — 分析決策
1. 規則引擎（thresholds 設定檔＋D1 檢核表）。
2. LLM 公告解析管線（資格條件結構化）。
3. D2 試算器 UI＋分析報告頁＋LINE 指令查詢。
   **驗收：任選 10 個真實案件，D1 紅黃綠燈判定與人工判讀一致率 ≥ 8/10；資格門檻（資本額、證照）抽取正確率逐案人工核對。**

### 需要 Allen 手動準備的事項（Agent 無法代辦）
- [ ] LINE 官方帳號申請＋Messaging API channel 開通（channel access token）
- [ ] 建立目標 LINE 群組並把官方帳號加入
- [ ] Firebase 專案建立（或提供既有專案）、Zeabur 帳號
- [ ] Anthropic API key（分析模組用）
- [ ] 政府電子採購網廠商帳號（後續電子領標/投標用，系統本身不需要）

---

## 6. 風險與邊界（誠實聲明）

1. **爬蟲被封是最大營運風險**：官方無即時 API，即時性完全依賴禮貌爬取。若被封，退路是 g0v API 或人工每日查詢＋系統只做記錄分析。
2. **公開頁資訊有限**：完整資格與規格在招標文件內，需領標才看得到。D1 只能做初篩，最終投標決策必讀文件——系統定位是「省掉 90% 不用看的案子」，不是取代讀標。
3. **獲利推估是粗估**：勞務成本模型參數（薪資、管理費率）需 Allen 依實際營運校準，前幾個月的推估僅供方向參考。
4. **法規門檻會修訂**：金額門檻做成設定檔，每年初人工核對工程會公告一次。
5. **LINE 訊息費用**：條件調鬆時訊息量會爆，Phase 1 就要做「多案合併摘要」機制。

---

## 7. 附錄：已驗證的參考資源

- 政府電子採購網：https://web.pcc.gov.tw/pis/
- 官方 OpenData 下載（XML，延遲 2 個月）：https://web.pcc.gov.tw/tps/tp/OpenData/showList
- g0v 標案 API（2025-07 起有 Cloudflare 擋 bot，用前實測）：https://pcc.g0v.ronny.tw/ ・ 原始碼 https://github.com/openfunltd/pcc.g0v.ronny.tw
- 招標查詢頁：https://web.pcc.gov.tw/prkms/tender/common/basic/indexTenderBasic
- LINE Notify 已於 2025-03-31 終止，改用 Messaging API：https://tech.havocfuture.tw/blog/line-notify-to-messaging-api
- 政府資料開放平臺相關集：https://data.gov.tw/suggests/88841
